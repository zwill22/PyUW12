# PyUW12

A python implementation of UW12 built using PySCF

To build and run pyuw12 with [uv](https://docs.astral.sh/uv/):
```bash
uv build
uv run pyuw12
```
To run the test suite use:
```bash
uv sync --extra test
uv run pytest
```
